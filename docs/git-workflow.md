# Git 工作流程与分支管理

**版本**: 1.0.0  
**更新时间**: 2026-01-15  
**状态**: Production Ready

---

## 🌳 分支策略

### 分支模型

采用 **GitFlow 简化版** + **主干开发**结合模式:

```
main (生产分支)
  ├── develop (开发主干)
  │   ├── feature/phase-3-messaging (功能分支)
  │   ├── feature/user-auth (功能分支)
  │   └── fix/token-refresh-bug (修复分支)
  ├── release/v0.2.0 (发布分支)
  └── hotfix/critical-security (热修复分支)
```

### 分支类型与规范

| 分支类型 | 命名规范 | 生命周期 | 合并目标 |
|---------|---------|---------|---------|
| **main** | `main` | 永久 | - |
| **develop** | `develop` | 永久 | main |
| **feature** | `feature/<描述>` | 临时 | develop |
| **fix** | `fix/<描述>` | 临时 | develop |
| **release** | `release/v<版本>` | 临时 | main + develop |
| **hotfix** | `hotfix/<描述>` | 临时 | main + develop |

### 分支详细说明

#### 1. main (主分支)

**用途**: 生产就绪代码,每个提交都是一个可发布版本

**保护规则**:
- ✅ 禁止直接推送
- ✅ 必须通过 PR
- ✅ 至少 1 人代码审查
- ✅ CI 必须通过
- ✅ 必须签名提交 (可选)

**标签策略**:
```bash
# 每次合并到 main 都打标签
git tag -a v0.1.0 -m "Release v0.1.0: Phase 1-2 完成"
git push origin v0.1.0
```

#### 2. develop (开发分支)

**用途**: 集成分支,所有功能开发的目标分支

**合并策略**:
- feature/* → develop (Squash merge 或 Merge commit)
- fix/* → develop (Squash merge)
- develop → main (Merge commit + 标签)

#### 3. feature/* (功能分支)

**命名示例**:
```
feature/phase-3-messaging
feature/user-cache-optimization
feature/prometheus-metrics
```

**工作流程**:
```bash
# 1. 从 develop 创建功能分支
git checkout develop
git pull origin develop
git checkout -b feature/phase-3-messaging

# 2. 开发功能
git add .
git commit -m "feat(messaging): 实现消息发送接口"

# 3. 定期同步 develop
git fetch origin develop
git rebase origin/develop

# 4. 推送到远程
git push -u origin feature/phase-3-messaging

# 5. 创建 PR: feature/phase-3-messaging → develop
# (通过 GitHub 界面)
```

#### 4. release/* (发布分支)

**用途**: 准备新版本发布,仅允许 bug 修复和版本号更新

**工作流程**:
```bash
# 1. 从 develop 创建发布分支
git checkout develop
git checkout -b release/v0.2.0

# 2. 更新版本号
echo "0.2.0" > VERSION
git commit -am "chore: bump version to 0.2.0"

# 3. 最后的 bug 修复
git commit -m "fix: 修复发布前发现的问题"

# 4. 合并到 main 并打标签
git checkout main
git merge --no-ff release/v0.2.0
git tag -a v0.2.0 -m "Release v0.2.0"

# 5. 合并回 develop
git checkout develop
git merge --no-ff release/v0.2.0

# 6. 删除发布分支
git branch -d release/v0.2.0
git push origin --delete release/v0.2.0
```

#### 5. hotfix/* (热修复分支)

**用途**: 紧急修复生产环境问题

**工作流程**:
```bash
# 1. 从 main 创建热修复分支
git checkout main
git checkout -b hotfix/critical-security-fix

# 2. 修复问题
git commit -m "fix(security): 修复 SQL 注入漏洞"

# 3. 合并到 main 并打补丁标签
git checkout main
git merge --no-ff hotfix/critical-security-fix
git tag -a v0.1.1 -m "Hotfix v0.1.1: 安全修复"

# 4. 合并回 develop
git checkout develop
git merge --no-ff hotfix/critical-security-fix

# 5. 删除热修复分支
git branch -d hotfix/critical-security-fix
```

---

## 🏷️ 标签规范 (CHK115)

### 语义化版本 (SemVer)

格式: `v<主版本>.<次版本>.<补丁版本>`

```
v0.1.0 - Phase 1-2 完成 (初始版本)
v0.2.0 - Phase 3 完成 (新增消息功能)
v0.2.1 - 修复 Token 刷新 Bug (补丁)
v0.3.0 - Phase 4 完成 (新增文档功能)
v1.0.0 - 生产就绪 (稳定版本)
```

### 版本号规则

| 版本类型 | 何时递增 | 示例 |
|---------|---------|------|
| **主版本** | 不兼容的 API 变更 | 0.x.x → 1.0.0 |
| **次版本** | 向后兼容的功能新增 | 0.1.x → 0.2.0 |
| **补丁版本** | 向后兼容的 bug 修复 | 0.1.0 → 0.1.1 |

### 标签创建命令

```bash
# 轻量标签 (不推荐)
git tag v0.1.0

# 附注标签 (推荐)
git tag -a v0.1.0 -m "Release v0.1.0: Phase 1-2 完成

- ✅ 配置管理系统
- ✅ Token 管理池
- ✅ SQLAlchemy 2.0 升级
- ✅ 77.33% 测试覆盖率
- ✅ 安全合规 (FR-077~095)
"

# 推送标签
git push origin v0.1.0

# 推送所有标签
git push origin --tags
```

### Phase 标签策略

| Phase | 标签 | 说明 |
|-------|------|------|
| Phase 1 | v0.1.0 | 基础设施完成 |
| Phase 2 | v0.1.0 | Token 管理完成 (同 Phase 1) |
| Phase 3 | v0.2.0 | 消息服务完成 |
| Phase 4 | v0.3.0 | 文档+通讯录完成 |
| Phase 5 | v0.4.0 | aPaaS 功能完成 |
| Stable | v1.0.0 | 生产就绪 |

---

## 📦 提交规范

### Conventional Commits

格式: `<类型>(<范围>): <描述>`

```
feat(messaging): 实现消息发送接口
fix(token): 修复 Token 刷新逻辑
docs(readme): 更新安装说明
style(core): 格式化代码
refactor(storage): 重构数据库连接池
test(cli): 添加 CLI 单元测试
chore(deps): 升级 SQLAlchemy 到 2.0.25
```

### 提交类型

| 类型 | 说明 | 示例 |
|------|------|------|
| **feat** | 新功能 | `feat(auth): 添加 OAuth 2.0 支持` |
| **fix** | Bug 修复 | `fix(token): 修复过期判断逻辑` |
| **docs** | 文档变更 | `docs(api): 更新 API 文档` |
| **style** | 代码格式 | `style: 运行 ruff format` |
| **refactor** | 重构 | `refactor(db): 优化查询性能` |
| **test** | 测试 | `test(unit): 添加 CredentialPool 测试` |
| **chore** | 构建/工具 | `chore(ci): 添加 GitHub Actions` |
| **perf** | 性能优化 | `perf(cache): 使用 Redis 缓存` |

### 提交描述规范

**好的提交**:
```bash
✅ feat(messaging): 实现群消息发送功能

- 支持文本、图片、卡片消息
- 实现批量发送接口
- 添加消息发送限流保护

Closes #42
```

**差的提交**:
```bash
❌ update
❌ fix bug
❌ 修改了一些文件
```

---

## 🔄 Phase 过渡流程 (CHK117)

### Phase 完成标准

#### Phase 1-2 完成检查清单

**功能完整性**:
- [x] 所有 T001-T015 任务完成
- [x] 所有验收标准达标
- [x] 无 Blocker 和 Major Gap

**质量标准**:
- [x] 测试覆盖率 ≥ 75%
- [x] Mypy 覆盖率 ≥ 99%
- [x] 0 个 Ruff 错误
- [x] CI 全部通过

**文档完整性**:
- [x] README 完善
- [x] API 文档完整
- [x] 架构文档更新
- [x] 部署文档完善

**安全合规**:
- [x] FR-077~095 全部实现
- [x] 依赖安全扫描通过
- [x] 容器安全扫描通过

#### Phase 过渡流程

```
Phase N 开发 → 自测试 → Code Review → CI/CD → Phase 验收 → 打标签 → 启动 Phase N+1
```

**详细步骤**:

1. **自测试** (开发者)
   ```bash
   # 运行测试
   pytest tests/ --cov=src
   
   # 检查类型
   mypy src/
   
   # 检查代码风格
   ruff check src/
   
   # 检查文档
   pydocstyle src/
   ```

2. **代码审查** (团队)
   - 创建 PR: `feature/phase-N → develop`
   - 至少 1 人审查
   - 解决所有评论

3. **CI/CD 验证** (自动)
   - 测试通过
   - 覆盖率达标
   - 代码质量检查通过
   - 安全扫描通过

4. **Phase 验收** (技术负责人)
   - 运行检查清单
   - 验证交付物
   - 更新文档

5. **打标签发布** (维护者)
   ```bash
   git checkout main
   git merge --no-ff develop
   git tag -a v0.N.0 -m "Release v0.N.0: Phase N 完成"
   git push origin main --tags
   ```

6. **启动下一 Phase**
   - 创建 Phase N+1 分支
   - 更新项目计划
   - 分配任务

### Phase 过渡条件矩阵

| 从 Phase | 到 Phase | 必要条件 | 建议条件 |
|---------|---------|---------|---------|
| Phase 1-2 | Phase 3 | 基础设施可用 + Token 管理正常 | 文档完善 |
| Phase 3 | Phase 4 | 消息发送功能完整 | 性能测试通过 |
| Phase 4 | Phase 5 | 文档+通讯录集成 | 用户反馈收集 |
| Phase 5 | v1.0 | 所有功能完成 + 生产验证 | 负载测试通过 |

---

## 📋 代码审查流程 (Code Review)

### 审查清单

#### 功能审查
- [ ] 功能实现符合需求
- [ ] 边界条件处理正确
- [ ] 错误处理完善
- [ ] 测试覆盖充分

#### 代码质量
- [ ] 命名清晰易懂
- [ ] 函数/类职责单一
- [ ] 避免代码重复
- [ ] 复杂度可接受
- [ ] 注释清晰充分

#### 类型安全
- [ ] 类型注解完整
- [ ] Mypy 检查通过
- [ ] 无 type: ignore (或有正当理由)

#### 安全性
- [ ] 无硬编码密钥
- [ ] 输入验证完善
- [ ] SQL 注入防护
- [ ] 敏感信息脱敏

#### 文档
- [ ] Docstring 完整 (Google Style)
- [ ] README 更新
- [ ] CHANGELOG 更新

### 审查流程

```
PR 创建 → 自动检查 → 人工审查 → 反馈修改 → 审批合并
```

**时效要求**:
- 小 PR (< 200 行): 24 小时内审查
- 中 PR (200-500 行): 48 小时内审查
- 大 PR (> 500 行): 72 小时内审查 (建议拆分)

### PR 模板

```markdown
## 变更类型
- [ ] Feature (新功能)
- [ ] Fix (Bug 修复)
- [ ] Docs (文档)
- [ ] Refactor (重构)
- [ ] Test (测试)

## 变更描述
[简要描述此 PR 的目的和内容]

## 相关 Issue
Closes #<issue_number>

## 测试
- [ ] 单元测试已添加/更新
- [ ] 集成测试已添加/更新
- [ ] 手动测试已完成

## 检查清单
- [ ] 代码符合项目规范
- [ ] 测试全部通过
- [ ] 文档已更新
- [ ] CHANGELOG 已更新

## 截图 (如适用)
[添加截图]
```

---

## 📦 交付物归档策略 (CHK116)

### 归档时机

| 事件 | 归档内容 | 位置 |
|------|---------|------|
| **Phase 完成** | 代码快照 + 文档 | Git Tag |
| **版本发布** | Release 包 + Changelog | GitHub Releases |
| **重大里程碑** | 完整项目备份 | 外部存储 |

### 归档内容

#### 1. 代码归档

```bash
# Git Tag (已包含代码快照)
git tag -a v0.1.0 -m "Phase 1-2 完成"

# 创建归档包
git archive --format=tar.gz --prefix=lark-service-v0.1.0/ v0.1.0 > lark-service-v0.1.0.tar.gz
```

#### 2. 文档归档

**归档结构**:
```
archives/
  └── v0.1.0/
      ├── source-code.tar.gz          # 源代码
      ├── docs/                       # 文档快照
      ├── specs/                      # 需求规范
      ├── test-reports/               # 测试报告
      ├── coverage-report.html        # 覆盖率报告
      ├── security-scan.json          # 安全扫描结果
      └── CHANGELOG.md                # 变更日志
```

#### 3. Release Notes

**示例**:
```markdown
# Release v0.1.0 - Phase 1-2 完成

**发布日期**: 2026-01-15  
**标签**: v0.1.0

## ✨ 新功能

- ✅ 配置管理系统 (SQLite + 加密)
- ✅ Token 管理池 (自动刷新 + 持久化)
- ✅ PostgreSQL Token 存储
- ✅ RabbitMQ 消息队列集成
- ✅ CLI 工具 (lark-service-cli)

## 🔧 改进

- SQLAlchemy 升级到 2.0 (现代语法)
- Mypy 覆盖率达到 99.8%
- 测试覆盖率达到 77.33%
- 安全合规 (FR-077~095)

## 📊 质量指标

- 测试: 140/140 通过
- 覆盖率: 77.33%
- Mypy: 99.8%
- Ruff: 0 错误

## 📚 文档

- 17 个文档 (8,515 行)
- 129 个需求条目
- 完整的 API 文档

## 🔗 资源

- [安装指南](docs/quickstart.md)
- [API 文档](docs/api.md)
- [架构设计](docs/architecture.md)

## 📦 下载

- Source code (tar.gz)
- Source code (zip)
```

### GitHub Releases 创建

```bash
# 使用 GitHub CLI
gh release create v0.1.0 \
  --title "Release v0.1.0 - Phase 1-2 完成" \
  --notes-file RELEASE_NOTES.md \
  lark-service-v0.1.0.tar.gz
```

---

## 🔧 常用命令

### 分支管理

```bash
# 查看所有分支
git branch -a

# 创建并切换分支
git checkout -b feature/new-feature

# 删除本地分支
git branch -d feature/old-feature

# 删除远程分支
git push origin --delete feature/old-feature

# 查看分支历史
git log --graph --oneline --all
```

### 标签管理

```bash
# 查看所有标签
git tag -l

# 查看标签详情
git show v0.1.0

# 删除本地标签
git tag -d v0.1.0

# 删除远程标签
git push origin --delete v0.1.0

# 检出特定标签
git checkout v0.1.0
```

### 同步更新

```bash
# 同步远程分支
git fetch origin

# 拉取并变基
git pull --rebase origin develop

# 推送本地分支
git push -u origin feature/my-feature
```

---

## ✅ 流程检查清单

### Git 工作流
- [ ] 分支策略已定义
- [ ] 分支命名规范已遵守
- [ ] 提交信息符合规范
- [ ] 代码已推送到远程

### 标签管理
- [ ] 版本号符合 SemVer
- [ ] 标签信息完整
- [ ] 标签已推送到远程

### Phase 过渡
- [ ] 完成标准已验证
- [ ] 检查清单已完成
- [ ] 文档已更新
- [ ] 标签已创建

### 代码审查
- [ ] PR 已创建
- [ ] 审查意见已处理
- [ ] CI 检查已通过
- [ ] 至少 1 人审批

### 交付归档
- [ ] Release Notes 已创建
- [ ] GitHub Release 已发布
- [ ] 归档包已生成
- [ ] 文档已归档

---

**维护者**: Lark Service Team  
**参考**: [git-workflow.md](./git-workflow.md)
