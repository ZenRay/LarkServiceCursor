# Staging环境部署检查清单

**版本**: 1.0.0
**更新时间**: 2026-01-18
**适用版本**: Lark Service v0.1.0

---

## 📋 部署前检查清单

### 1. 环境准备 ✓

#### 1.1 服务器配置
- [ ] 服务器已准备就绪 (建议: 2核4GB以上)
- [ ] 操作系统: Ubuntu 20.04+ / CentOS 8+ / Debian 11+
- [ ] Python 3.11+ 已安装
- [ ] uv 包管理器已安装
- [ ] Git 已安装
- [ ] 网络连通性: 可访问飞书API (open.feishu.cn)
- [ ] 防火墙规则已配置

#### 1.2 依赖服务
- [ ] **PostgreSQL 13+** 已部署
  - [ ] 数据库 `lark_service_staging` 已创建
  - [ ] 用户 `lark_staging` 已创建并授权
  - [ ] `pgcrypto` 扩展已启用
  - [ ] 连接池参数已配置 (参考 deployment.md §2.1)
  - [ ] SSL 连接已启用 (推荐)
  - [ ] 备份策略已配置

- [ ] **RabbitMQ** (可选, 如使用消息队列)
  - [ ] RabbitMQ 3.x 已部署
  - [ ] Virtual host `/lark-staging` 已创建
  - [ ] 用户已创建并授权

#### 1.3 监控和日志
- [ ] 日志聚合工具已配置 (ELK/Splunk/CloudWatch等)
- [ ] Metrics 收集已配置 (Prometheus/Grafana等)
- [ ] 告警规则已设置
- [ ] Sentry 错误追踪已配置 (可选)

### 2. 代码部署 ✓

#### 2.1 代码获取
```bash
# 克隆代码仓库
cd /opt
git clone <repository-url> lark-service
cd lark-service
git checkout 001-lark-service-core  # 或指定的release tag

# 验证代码完整性
git log --oneline -5
git status
```

- [ ] 代码已克隆到服务器
- [ ] 分支/标签正确
- [ ] 代码完整性已验证

#### 2.2 虚拟环境和依赖
```bash
# 创建虚拟环境
uv venv .venv-staging

# 激活虚拟环境
source .venv-staging/bin/activate

# 安装生产依赖 (使用精确版本锁定的requirements-prod.txt)
uv pip install -r requirements-prod.txt

# 验证安装
python -c "import lark_service; print(lark_service.__version__)"
```

- [ ] 虚拟环境已创建
- [ ] 生产依赖已安装 (使用 requirements-prod.txt)
- [ ] 依赖版本已验证
- [ ] 无editable安装

#### 2.3 环境配置
```bash
# 复制配置模板
cp config/staging.env.template .env.staging

# 编辑配置文件 (填写实际值)
vim .env.staging

# 验证配置文件
python scripts/validate_env.py .env.staging
```

- [ ] 配置文件已创建 (.env.staging)
- [ ] 所有必需配置项已填写
- [ ] 密钥已替换 (不使用示例值)
- [ ] 配置文件权限已设置 (600)
- [ ] 配置文件已验证

### 3. 数据库初始化 ✓

#### 3.1 数据库连接测试
```bash
# 激活环境
source .venv-staging/bin/activate

# 加载环境变量
export $(cat .env.staging | grep -v '^#' | xargs)

# 测试数据库连接
python scripts/test_db_connection.py
```

- [ ] 数据库连接成功
- [ ] pgcrypto扩展可用
- [ ] 用户权限正确

#### 3.2 数据库迁移
```bash
# 查看当前数据库版本
alembic current

# 查看待执行的迁移
alembic history

# 执行迁移到最新版本
alembic upgrade head

# 验证迁移结果
alembic current
psql -h $DB_HOST -U $DB_USER -d $DB_NAME -c "\dt"
```

- [ ] 迁移脚本已执行
- [ ] 所有表已创建 (tokens, user_cache, user_auth_sessions)
- [ ] 索引已创建
- [ ] 迁移版本正确

### 4. 应用配置 ✓

#### 4.1 飞书应用配置
```bash
# 添加飞书应用配置 (使用CLI)
lark-service-cli app add \
  --app-id <your-app-id> \
  --app-secret <your-app-secret> \
  --name "Staging App" \
  --description "Staging environment application"

# 验证配置
lark-service-cli app list
lark-service-cli app show <your-app-id>

# 启用应用
lark-service-cli app enable <your-app-id>
```

- [ ] 飞书应用已添加
- [ ] App ID 和 App Secret 正确
- [ ] 应用已启用
- [ ] Token获取成功

### 5. 功能验证 ✓

#### 5.1 基础功能测试
```bash
# 运行健康检查脚本
python scripts/staging_health_check.py

# 测试Token获取
python -c "
from lark_service.credential import CredentialPool
pool = CredentialPool(app_id='<your-app-id>')
token = pool.get_app_access_token()
print(f'Token获取成功: {token[:20]}...')
"

# 测试消息发送 (可选)
python scripts/test_send_message.py
```

- [ ] 健康检查通过
- [ ] Token获取成功
- [ ] Token自动刷新正常
- [ ] 基础API调用成功

#### 5.2 单元测试
```bash
# 运行单元测试 (staging环境)
pytest tests/unit/ -v --cov=src/lark_service

# 查看覆盖率
pytest tests/unit/ --cov=src/lark_service --cov-report=html
```

- [ ] 所有单元测试通过
- [ ] 覆盖率 ≥ 60%

#### 5.3 集成测试
```bash
# 运行集成测试
pytest tests/integration/ -v
```

- [ ] 所有集成测试通过
- [ ] 数据库交互正常
- [ ] API调用正常

### 6. 性能测试 ✓

#### 6.1 压力测试
```bash
# 运行Locust性能测试
locust -f tests/performance/load_test.py \
  --host=http://staging-host:port \
  --users=100 \
  --spawn-rate=10 \
  --run-time=10m \
  --html=load_test_report.html
```

- [ ] 性能测试已执行
- [ ] P95延迟 < 500ms ✓
- [ ] 吞吐量 > 1000 req/s ✓
- [ ] 错误率 < 0.1% ✓
- [ ] 资源使用正常 (CPU < 70%, 内存 < 80%)

#### 6.2 性能基准记录
- [ ] 性能测试报告已生成
- [ ] 基准数据已记录
- [ ] 性能瓶颈已识别 (如有)

### 7. 数据安全验证 ✓

#### 7.1 数据库备份测试
```bash
# 运行备份脚本
bash scripts/backup_database.sh

# 验证备份文件
ls -lh $BACKUP_DIR/
```

- [ ] 备份脚本执行成功
- [ ] 备份文件已生成
- [ ] 备份文件已加密 (如启用)
- [ ] 备份文件大小合理

#### 7.2 数据库恢复测试
```bash
# 创建测试数据库
createdb -h $DB_HOST -U $DB_USER lark_service_staging_restore_test

# 运行恢复脚本
bash scripts/restore_database.sh \
  --backup-file=$BACKUP_DIR/latest_backup.sql.gz \
  --target-db=lark_service_staging_restore_test

# 验证恢复结果
psql -h $DB_HOST -U $DB_USER -d lark_service_staging_restore_test -c "\dt"
psql -h $DB_HOST -U $DB_USER -d lark_service_staging_restore_test -c "SELECT COUNT(*) FROM tokens;"

# 清理测试数据库
dropdb -h $DB_HOST -U $DB_USER lark_service_staging_restore_test
```

- [ ] 恢复脚本执行成功
- [ ] 数据完整性已验证
- [ ] RTO ≤ 4小时 ✓
- [ ] RPO ≤ 1小时 ✓

#### 7.3 迁移回滚测试
```bash
# 运行回滚测试脚本
bash scripts/test_migration_rollback.sh
```

- [ ] 回滚测试通过
- [ ] 数据一致性已验证
- [ ] 回滚文档已确认

### 8. 监控和可观测性 ✓

#### 8.1 日志验证
```bash
# 查看应用日志
tail -f /var/log/lark-service/app.log

# 验证日志格式 (JSON)
cat /var/log/lark-service/app.log | jq .

# 验证日志级别
grep "ERROR" /var/log/lark-service/app.log
```

- [ ] 日志文件可访问
- [ ] 日志格式正确 (JSON)
- [ ] 日志级别正确 (INFO)
- [ ] 敏感信息已脱敏
- [ ] request_id 已记录

#### 8.2 Metrics验证
```bash
# 访问metrics端点
curl http://localhost:9090/metrics

# 验证关键指标
curl http://localhost:9090/metrics | grep lark_service
```

- [ ] Metrics端点可访问
- [ ] Prometheus格式正确
- [ ] 关键指标存在 (请求数、延迟、错误率等)

#### 8.3 告警配置
- [ ] CPU使用率告警 (阈值: 80%)
- [ ] 内存使用率告警 (阈值: 85%)
- [ ] 错误率告警 (阈值: 1%)
- [ ] API延迟告警 (P95 > 500ms)
- [ ] 数据库连接池耗尽告警
- [ ] 磁盘空间告警 (阈值: 85%)

### 9. 安全检查 ✓

#### 9.1 密钥安全
- [ ] 所有密钥已轮换 (不使用开发环境密钥)
- [ ] 密钥文件权限正确 (600)
- [ ] 密钥未提交到版本控制
- [ ] 密钥管理流程已文档化

#### 9.2 网络安全
- [ ] 防火墙规则已配置
- [ ] 仅必要端口对外开放
- [ ] SSL/TLS已启用
- [ ] CORS配置正确 (如适用)

#### 9.3 依赖安全
```bash
# 运行安全扫描
pip-audit
bandit -r src/

# 检查依赖漏洞
safety check
```

- [ ] 无高危漏洞
- [ ] 无中危漏洞 (或已评估风险)
- [ ] 依赖版本最新

### 10. 文档和流程 ✓

#### 10.1 运维文档
- [ ] 部署文档已更新 (deployment.md)
- [ ] 运维手册已准备
- [ ] 故障排查手册已准备
- [ ] 回滚流程已文档化

#### 10.2 应急流程
- [ ] 紧急回滚流程已演练
- [ ] 应急联系人已确定
- [ ] On-call轮值已安排
- [ ] 事故响应流程已文档化

---

## 🚀 部署执行

### 部署步骤

1. **前置检查** (30分钟)
   - 完成上述 §1-§2 所有检查项
   - 确认依赖服务就绪

2. **代码部署** (30分钟)
   - 部署代码到staging服务器
   - 安装依赖
   - 配置环境变量

3. **数据库初始化** (15分钟)
   - 执行数据库迁移
   - 验证迁移结果

4. **应用配置** (15分钟)
   - 配置飞书应用
   - 验证Token获取

5. **功能验证** (1小时)
   - 运行测试套件
   - 验证核心功能

6. **性能测试** (1小时)
   - 运行压力测试
   - 记录基准数据

7. **安全验证** (30分钟)
   - 数据库备份恢复测试
   - 迁移回滚测试

8. **监控配置** (30分钟)
   - 配置日志聚合
   - 配置告警规则
   - 验证metrics采集

**总计**: 约4.5小时

---

## 📊 验收标准

### 功能验收
- ✓ 所有单元测试通过 (覆盖率 ≥ 60%)
- ✓ 所有集成测试通过
- ✓ Token自动获取和刷新正常
- ✓ 消息发送功能正常
- ✓ 数据库读写正常

### 性能验收
- ✓ P95延迟 < 500ms
- ✓ 吞吐量 > 1000 req/s
- ✓ 错误率 < 0.1%
- ✓ CPU使用率 < 70%
- ✓ 内存使用率 < 80%

### 安全验收
- ✓ 无高危安全漏洞
- ✓ 数据库备份恢复成功 (RTO ≤ 4h, RPO ≤ 1h)
- ✓ 迁移回滚测试通过
- ✓ SSL/TLS已启用
- ✓ 密钥安全已验证

### 监控验收
- ✓ 日志采集正常
- ✓ Metrics采集正常
- ✓ 告警规则已配置
- ✓ 告警通知渠道已验证

---

## 📝 部署后观察期 (1-2周)

### 每日检查
- [ ] 检查应用日志 (错误日志)
- [ ] 检查系统资源使用 (CPU/内存/磁盘)
- [ ] 检查数据库连接数
- [ ] 检查API错误率
- [ ] 检查告警通知

### 每周检查
- [ ] 回顾性能指标趋势
- [ ] 回顾错误日志
- [ ] 验证备份执行情况
- [ ] 检查依赖安全更新
- [ ] 团队回顾会议

### 观察期结束
- [ ] 性能稳定 (2周内无重大波动)
- [ ] 无P0/P1级别故障
- [ ] 团队熟悉运维流程
- [ ] 准备生产环境部署

---

## 🆘 回滚流程

如果发现重大问题需要回滚:

### 1. 应用回滚
```bash
# 切换到上一个稳定版本
cd /opt/lark-service
git checkout <previous-stable-tag>

# 重启应用 (如果是独立服务)
systemctl restart lark-service
```

### 2. 数据库回滚
```bash
# 参考: docs/database-migration-rollback.md
alembic downgrade <target-version>
```

### 3. 配置回滚
```bash
# 恢复上一个配置文件
cp .env.staging.backup .env.staging
```

### 4. 验证回滚
- 验证应用功能正常
- 验证数据一致性
- 通知相关团队

**RTO目标**: 15分钟内完成回滚

---

## 📞 联系信息

**项目负责人**: Ray
**环境**: Staging
**部署日期**: _填写部署日期_
**部署版本**: v0.1.0

**参考文档**:
- `docs/deployment.md` - 部署指南
- `docs/database-migration-rollback.md` - 数据库回滚指南
- `CURRENT-STATUS.md` - 项目当前状态
- `specs/001-lark-service-core/checklists/production-readiness-evaluation-summary.md` - 生产就绪评估报告

---

**检查清单版本**: 1.0.0
**最后更新**: 2026-01-18
