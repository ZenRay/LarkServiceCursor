# Prometheus 配置文件
# 用于采集 Lark Service metrics

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    environment: 'staging-local'
    project: 'lark-service'

# Alertmanager 配置 (可选)
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets: ['alertmanager:9093']

# 抓取配置
scrape_configs:
  # Prometheus 自身
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Lark Service 应用 metrics
  # 注意: 需要应用暴露 metrics 端点
  - job_name: 'lark-service'
    static_configs:
      - targets: ['host.docker.internal:9091']  # 应用metrics端口
    metrics_path: '/metrics'
    scrape_interval: 10s

  # PostgreSQL Exporter (可选)
  # - job_name: 'postgres'
  #   static_configs:
  #     - targets: ['postgres-exporter:9187']

  # RabbitMQ (内置prometheus插件)
  - job_name: 'rabbitmq'
    static_configs:
      - targets: ['rabbitmq:15692']
    metrics_path: '/metrics'

  # Redis Exporter (可选)
  # - job_name: 'redis'
  #   static_configs:
  #     - targets: ['redis-exporter:9121']
